# Sprint 15A — Propuestas PDF Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar generación de PDF al comando `/documento` para que entregue tanto `.docx` como `.pdf` en el mismo mensaje de Telegram.

**Architecture:** Se añade `_markdown_to_pdf()` en `document_tools.py` usando `fpdf2`. `GenerateDocumentTool.execute()` genera ambos formatos y los guarda en `metadata`. `cmd_documento` en el bot envía los dos archivos en secuencia.

**Tech Stack:** `fpdf2` (pure Python, sin dependencias de sistema), python-telegram-bot, Django 5.2.

---

## File Structure

| Archivo | Cambio |
|---|---|
| `requirements.txt` | Añadir `fpdf2>=2.7.9` |
| `core/agent/infrastructure/tools/document_tools.py` | Añadir `_markdown_to_pdf()`, modificar `GenerateDocumentTool.execute()` |
| `core/agent/management/commands/run_telegram_bot.py` | Modificar `cmd_documento` para enviar PDF también |
| `core/agent/tests/test_sprint15_pdf.py` | Nuevo archivo de tests |

---

### Task 1: Añadir dependencia fpdf2 y escribir tests fallidos

**Files:**
- Modify: `requirements.txt`
- Create: `core/agent/tests/test_sprint15_pdf.py`

- [ ] **Step 1: Añadir fpdf2 a requirements.txt**

Abrir `requirements.txt` y añadir la línea (junto a `python-docx==1.1.2`):
```
fpdf2>=2.7.9
```

- [ ] **Step 2: Escribir los tests fallidos**

Crear `core/agent/tests/test_sprint15_pdf.py`:

```python
"""Tests Sprint 15A — Generación de PDF en document_tools."""
import pytest
from unittest.mock import patch, MagicMock
from django.test import override_settings

pytestmark = pytest.mark.django_db


FAKE_SETTINGS = {
    'GEMINI_API_KEY': 'test-key',
    'AI_MODEL': 'gemini-3.1-flash-lite',
}


class TestMarkdownToPdf:
    def test_returns_bytes(self):
        """_markdown_to_pdf devuelve bytes (PDF válido)."""
        from core.agent.infrastructure.tools.document_tools import _markdown_to_pdf
        result = _markdown_to_pdf('# Título\n\nContenido de prueba.')
        assert isinstance(result, bytes)
        assert result[:4] == b'%PDF'  # Magic bytes de PDF

    def test_handles_headings(self):
        """_markdown_to_pdf procesa H1, H2, H3 sin error."""
        from core.agent.infrastructure.tools.document_tools import _markdown_to_pdf
        text = '# H1\n## H2\n### H3\nPárrafo normal.'
        result = _markdown_to_pdf(text)
        assert isinstance(result, bytes)
        assert len(result) > 100

    def test_handles_bold_text(self):
        """_markdown_to_pdf procesa **texto en negrita** sin error."""
        from core.agent.infrastructure.tools.document_tools import _markdown_to_pdf
        result = _markdown_to_pdf('**Importante:** este texto está en negrita.')
        assert isinstance(result, bytes)

    def test_handles_spanish_chars(self):
        """_markdown_to_pdf maneja caracteres españoles (á, é, ñ, ¿, ¡)."""
        from core.agent.infrastructure.tools.document_tools import _markdown_to_pdf
        result = _markdown_to_pdf('# Propuesta de negocio\n\nBienvenido. Análisis específico para México.')
        assert isinstance(result, bytes)
        assert result[:4] == b'%PDF'

    def test_handles_empty_lines(self):
        """_markdown_to_pdf no falla con líneas vacías."""
        from core.agent.infrastructure.tools.document_tools import _markdown_to_pdf
        result = _markdown_to_pdf('\n\n# Título\n\n\nContenido.\n\n')
        assert isinstance(result, bytes)


class TestGenerateDocumentToolPdf:
    @override_settings(**FAKE_SETTINGS)
    def test_execute_returns_pdf_bytes_in_metadata(self):
        """GenerateDocumentTool.execute() incluye pdf_bytes en metadata."""
        from core.agent.infrastructure.tools.document_tools import GenerateDocumentTool
        tool = GenerateDocumentTool()
        fake_content = '# Propuesta\n\n## Introducción\n\nContenido de la propuesta.'
        with patch.object(tool._gemini, 'generate_response', return_value=fake_content):
            result = tool.execute(doc_type='propuesta', description='Identidad de marca')
        assert result.success is True
        assert 'pdf_bytes' in result.metadata
        assert isinstance(result.metadata['pdf_bytes'], bytes)
        assert result.metadata['pdf_bytes'][:4] == b'%PDF'

    @override_settings(**FAKE_SETTINGS)
    def test_execute_returns_pdf_filename(self):
        """GenerateDocumentTool.execute() incluye pdf_filename en metadata."""
        from core.agent.infrastructure.tools.document_tools import GenerateDocumentTool
        tool = GenerateDocumentTool()
        with patch.object(tool._gemini, 'generate_response', return_value='# Propuesta\n\nTexto.'):
            result = tool.execute(doc_type='propuesta', description='Test')
        assert 'pdf_filename' in result.metadata
        assert result.metadata['pdf_filename'].endswith('.pdf')
        assert 'propuesta' in result.metadata['pdf_filename']

    @override_settings(**FAKE_SETTINGS)
    def test_execute_still_returns_docx_bytes(self):
        """GenerateDocumentTool.execute() sigue retornando docx_bytes (no regresión)."""
        from core.agent.infrastructure.tools.document_tools import GenerateDocumentTool
        tool = GenerateDocumentTool()
        with patch.object(tool._gemini, 'generate_response', return_value='# Doc\n\nTexto.'):
            result = tool.execute(doc_type='informe', description='Test')
        assert 'docx_bytes' in result.metadata
        assert isinstance(result.metadata['docx_bytes'], bytes)
```

- [ ] **Step 3: Verificar que los tests fallan**

```bash
docker exec chatbot-backend-1 python -m pytest core/agent/tests/test_sprint15_pdf.py -v 2>&1 | tail -20
```

Resultado esperado: `FAILED` con `ImportError: cannot import name '_markdown_to_pdf'` o `ModuleNotFoundError: No module named 'fpdf'`.

- [ ] **Step 4: Commit**

```bash
GIT_EDITOR=true git add requirements.txt core/agent/tests/test_sprint15_pdf.py
GIT_EDITOR=true git commit -m "test(sprint15a): failing tests for PDF generation"
```

---

### Task 2: Implementar `_markdown_to_pdf()` en document_tools.py

**Files:**
- Modify: `core/agent/infrastructure/tools/document_tools.py`

Instalar fpdf2 primero en el contenedor (build se hace en Task 4):
```bash
docker exec chatbot-backend-1 pip install fpdf2>=2.7.9
```

- [ ] **Step 1: Añadir import y función `_markdown_to_pdf`**

En `core/agent/infrastructure/tools/document_tools.py`, al inicio del archivo (después de los imports existentes), añadir:

```python
import io
import logging
import re
from datetime import date
from django.conf import settings
from docx import Document
from fpdf import FPDF
from core.agent.domain.tools import BaseTool, ToolResult
from core.agent.infrastructure.gemini_adapter import GeminiAdapter, FALLBACK_MESSAGE
from core.agent.infrastructure.rag_utils import get_rag_context
```

Reemplazar el import existente de `io` (ya está) y añadir `from fpdf import FPDF`.

Después de la función `_markdown_to_docx`, añadir:

```python
def _markdown_to_pdf(text: str) -> bytes:
    """Convierte markdown simple (# ## ### **bold**) a PDF con fpdf2."""
    pdf = FPDF()
    pdf.set_auto_page_break(auto=True, margin=15)
    pdf.set_margins(20, 20, 20)
    pdf.add_page()

    for line in text.split('\n'):
        line = line.strip()
        if not line:
            pdf.ln(4)
            continue
        if line.startswith('### '):
            pdf.set_font('Helvetica', 'B', 13)
            pdf.multi_cell(0, 8, line[4:])
            pdf.ln(2)
        elif line.startswith('## '):
            pdf.set_font('Helvetica', 'B', 15)
            pdf.multi_cell(0, 10, line[3:])
            pdf.ln(3)
        elif line.startswith('# '):
            pdf.set_font('Helvetica', 'B', 18)
            pdf.multi_cell(0, 12, line[2:])
            pdf.ln(4)
        else:
            parts = re.split(r'(\*\*.*?\*\*)', line)
            pdf.set_x(pdf.l_margin)
            for part in parts:
                if part.startswith('**') and part.endswith('**'):
                    pdf.set_font('Helvetica', 'B', 11)
                    pdf.write(6, part[2:-2])
                else:
                    pdf.set_font('Helvetica', '', 11)
                    if part:
                        pdf.write(6, part)
            pdf.ln(6)

    return bytes(pdf.output())
```

- [ ] **Step 2: Correr tests de `_markdown_to_pdf`**

```bash
docker exec chatbot-backend-1 python -m pytest core/agent/tests/test_sprint15_pdf.py::TestMarkdownToPdf -v
```

Resultado esperado: 5 PASSED.

- [ ] **Step 3: Commit**

```bash
GIT_EDITOR=true git add core/agent/infrastructure/tools/document_tools.py
GIT_EDITOR=true git commit -m "feat(sprint15a): add _markdown_to_pdf() using fpdf2"
```

---

### Task 3: Actualizar `GenerateDocumentTool.execute()` para retornar PDF

**Files:**
- Modify: `core/agent/infrastructure/tools/document_tools.py`

- [ ] **Step 1: Modificar el método `execute()` de `GenerateDocumentTool`**

Localizar el bloque final del método `execute()` (líneas 85-98 aproximadamente) y reemplazar:

```python
        doc = Document()
        _markdown_to_docx(doc, content)

        buf = io.BytesIO()
        doc.save(buf)
        docx_bytes = buf.getvalue()

        filename = f'{doc_type}_{date.today().isoformat()}.docx'
        return ToolResult(
            content=f'Documento "{filename}" generado correctamente.',
            tool_name=self.name,
            success=True,
            metadata={'docx_bytes': docx_bytes, 'filename': filename, 'doc_type': doc_type},
        )
```

con:

```python
        doc = Document()
        _markdown_to_docx(doc, content)
        buf = io.BytesIO()
        doc.save(buf)
        docx_bytes = buf.getvalue()

        pdf_bytes = _markdown_to_pdf(content)

        filename = f'{doc_type}_{date.today().isoformat()}.docx'
        pdf_filename = f'{doc_type}_{date.today().isoformat()}.pdf'
        return ToolResult(
            content=f'Documentos "{filename}" y "{pdf_filename}" generados correctamente.',
            tool_name=self.name,
            success=True,
            metadata={
                'docx_bytes': docx_bytes,
                'filename': filename,
                'pdf_bytes': pdf_bytes,
                'pdf_filename': pdf_filename,
                'doc_type': doc_type,
            },
        )
```

- [ ] **Step 2: Correr todos los tests de PDF**

```bash
docker exec chatbot-backend-1 python -m pytest core/agent/tests/test_sprint15_pdf.py -v
```

Resultado esperado: 8 PASSED.

- [ ] **Step 3: Correr tests de regresión de document_tools**

```bash
docker exec chatbot-backend-1 python -m pytest core/agent/tests/test_tools.py -v -k "document"
```

Resultado esperado: todos los tests existentes de document PASSED.

- [ ] **Step 4: Commit**

```bash
GIT_EDITOR=true git add core/agent/infrastructure/tools/document_tools.py
GIT_EDITOR=true git commit -m "feat(sprint15a): GenerateDocumentTool now returns both docx and pdf"
```

---

### Task 4: Actualizar `cmd_documento` para enviar ambos archivos

**Files:**
- Modify: `core/agent/management/commands/run_telegram_bot.py`

El archivo es grande. Localizar `cmd_documento` (línea ~587) y la sección que envía el archivo.

- [ ] **Step 1: Modificar el bloque de envío en `cmd_documento`**

Localizar este bloque dentro de `cmd_documento`:

```python
    if result:
        if result.success:
            docx_bytes = result.metadata.get('docx_bytes')
            filename = result.metadata.get('filename', 'documento.docx')
            import io
            await update.message.reply_document(
                document=io.BytesIO(docx_bytes),
                filename=filename,
                caption=f'✅ {result.content}',
            )
        else:
            await update.message.reply_text(f'❌ {result.content}')
```

Reemplazarlo con:

```python
    if result:
        if result.success:
            docx_bytes = result.metadata.get('docx_bytes')
            filename = result.metadata.get('filename', 'documento.docx')
            pdf_bytes = result.metadata.get('pdf_bytes')
            pdf_filename = result.metadata.get('pdf_filename', 'documento.pdf')
            import io
            await update.message.reply_document(
                document=io.BytesIO(docx_bytes),
                filename=filename,
                caption='📄 Word (.docx) — editable',
            )
            if pdf_bytes:
                await update.message.reply_document(
                    document=io.BytesIO(pdf_bytes),
                    filename=pdf_filename,
                    caption='📋 PDF — listo para compartir',
                )
        else:
            await update.message.reply_text(f'❌ {result.content}')
```

- [ ] **Step 2: Reconstruir el contenedor backend para incluir fpdf2**

```bash
docker compose build backend && docker compose up -d backend
```

Resultado esperado: build exitoso, contenedor reiniciado.

- [ ] **Step 3: Correr todos los tests del sprint**

```bash
docker exec chatbot-backend-1 python -m pytest core/agent/tests/test_sprint15_pdf.py -v
```

Resultado esperado: 8 PASSED.

- [ ] **Step 4: Correr la suite completa para detectar regresiones**

```bash
docker exec chatbot-backend-1 python -m pytest core/agent/tests/ -q 2>&1 | tail -5
```

Resultado esperado: todos pasan (mínimo 272 + 8 nuevos = 280 PASSED, 0 failed).

- [ ] **Step 5: Commit**

```bash
GIT_EDITOR=true git add core/agent/management/commands/run_telegram_bot.py requirements.txt
GIT_EDITOR=true git commit -m "feat(sprint15a): cmd_documento sends both docx and pdf files"
```

---

## Notas de validación

Probar en Telegram con:
```
/documento propuesta Identidad visual para restaurante de comida mexicana
```

Resultado esperado: el bot responde con 2 archivos — uno `.docx` y uno `.pdf`.
